<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.miwok">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NumbersActivity"
            android:theme="@style/Numbers"
            android:label="Numbers"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".FamilyActivity"
            android:theme="@style/FamilyMembers"
            android:label="Family Members"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".ColorsActivity"
            android:theme="@style/Colors"
            android:label="Colors"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".PhrasesActivity"
            android:theme="@style/Phrases"
            android:label="Phrases"
            android:parentActivityName=".MainActivity"/>
    </application>

</manifest>